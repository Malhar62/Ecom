import { ViewStyle, TextStyle } from 'react-native'
import { HEIGHT } from '../../theme/scale'
export const searchStyles = {
    txtip: {
        width: '80%',
        height: HEIGHT(40),
        backgroundColor: '#fff',
        flexDirection: 'row',
        alignSelf: 'center',
        alignItems: 'center',
        position: 'absolute',
        bottom: 20
    } as ViewStyle
}